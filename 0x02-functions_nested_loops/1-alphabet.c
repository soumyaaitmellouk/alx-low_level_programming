#include <stdio.h>
/**
*main - Entry point
*Description: c program print a sentance.
*Return: Always 0 (Success)
*/
int main(void)
void print_alphabet(void);
{
int n=97;
while (n <= 122)
{
putchar(n);
n++;
}
putchar('\n');
return (0);
}
